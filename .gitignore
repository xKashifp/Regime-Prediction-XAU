__pycache__/
*.pyc

# Live SQLite DB + raw CSV history -- large, binary, changes every tick.
# Regenerated by csv_seed.py / the live loops, not meant to be versioned.
data/

# Rotating log files
logs/

# Trained model artifacts -- regenerated by train_model.py /
# train_intraday_model.py / train_near_term_model.py.
models/

# Supervisor pid files (runtime state, not source)
run/

.DS_Store
Thumbs.db
